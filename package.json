{
  "name": "de-ship",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ de-ship",
    "create-local": "graph create --node http://localhost:8020/ de-ship",
    "remove-local": "graph remove --node http://localhost:8020/ de-ship",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 de-ship",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.0",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": { "matchstick-as": "0.6.0" }
}
